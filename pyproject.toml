[tool.poetry]
name = "mkdocs_sandbox"
version = "0.1.0"
description = "Experimentation and testing mkdocs"
authors = ["Malcolm Harper <ve2ddz@ve2ddz.ca>"]

[tool.poetry.dependencies]
python = "^3.8"
mkdocs = "^1.2.3"

[tool.poetry.dev-dependencies]

[build-system]
requires = ["poetry-core>=1.0.0"]
build-backend = "poetry.core.masonry.api"
